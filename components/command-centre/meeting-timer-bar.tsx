'use client'

// Timer is now rendered inline in HeroHeader next to the Start Meeting button.
// This component is kept for import compatibility but renders nothing.
export function MeetingTimerBar() {
  return null
}
