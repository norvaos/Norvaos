/**
 * Re-export adapter types from the main types module.
 * Platform adapters import from here for convenience.
 */

export type {
  SourcePlatform,
  ImportEntityType,
  FieldMapping,
  EntityAdapter,
  PlatformAdapter,
} from '../types'
